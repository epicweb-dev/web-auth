# Load the User

👨‍💼 We're almost there! Now you need to update
the <InlineFile file="app/root.tsx" /> so it reads the `userId` from the session
storage and loads the user from the database. Then we can send the user along
to the UI and display some user-specific information.

The emoji will be there to help you through this one!
